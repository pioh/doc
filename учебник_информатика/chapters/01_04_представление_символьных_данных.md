# Глава 1.4: Представление символьных данных в двоичном коде

## Введение

Текстовая информация составляет значительную часть всех данных, обрабатываемых современными компьютерами. От простых текстовых документов и исходного кода программ до веб-страниц, электронных книг и сообщений в социальных сетях — везде используется представление символов в двоичном коде. В отличие от чисел, которые имеют естественное математическое представление, символы — это абстрактные сущности, требующие договорённости о способе их кодирования.

История кодирования текста наглядно демонстрирует эволюцию информационных технологий: от простейшей 7-битной ASCII, достаточной для английского языка, до универсального Unicode, охватывающего письменности всех народов мира, включая древние языки и современные эмодзи. Понимание принципов текстового кодирования критически важно для разработчиков программного обеспечения, так как неправильный выбор кодировки приводит к печально известной проблеме «кракозябр» — нечитаемых символов на месте текста.

Эта глава тесно связана с главой 1.2 (представление данных в памяти ЭВМ), так как мы будем рассматривать, сколько байтов занимают текстовые данные в различных кодировках. Также знания из главы 1.1 (двоичная арифметика) помогут понять расчёты размеров текстовых файлов.

## Основные понятия кодирования текста

### Что такое символ и кодировка

**Символ** — это минимальная единица текста: буква, цифра, знак пунктуации, пробел, специальный знак и т.д.

**Кодировка (character encoding)** — это система правил, устанавливающая взаимно-однозначное соответствие между символами некоторого алфавита и их числовыми кодами (обычно последовательностями битов). Кодировка определяет, какое двоичное число соответствует каждому символу.

Важно понимать различие между:
- **Набором символов (character set)** — перечнем символов, которые могут быть закодированы
- **Схемой кодирования (encoding scheme)** — способом представления этих символов в виде последовательностей байтов

### Необходимость стандартизации

В первые десятилетия эры компьютеров каждый производитель использовал свою собственную систему кодирования символов, что делало обмен текстовыми данными между различными системами крайне затруднительным. Необходимость в стандартизации привела к созданию ASCII — первой широко распространённой универсальной кодировки.

## Исторические кодировки

### ASCII (American Standard Code for Information Interchange)

**ASCII** — американский стандартный код для обмена информацией, разработанный в 1963 году. Это 7-битная кодировка, позволяющая представить 2⁷ = 128 различных символов.

Структура ASCII:
- **Коды 0-31**: управляющие символы (непечатаемые): NULL, LINE FEED, CARRIAGE RETURN и т.д.
- **Код 32**: пробел
- **Коды 33-47**: знаки пунктуации и специальные символы (!, ", #, $, %, &, ', (, ), *, +, ,, -, ., /)
- **Коды 48-57**: цифры от '0' до '9'
- **Коды 58-64**: дополнительные символы (:, ;, <, =, >, ?, @)
- **Коды 65-90**: прописные латинские буквы от 'A' до 'Z'
- **Коды 91-96**: дополнительные символы ([, \, ], ^, _, `)
- **Коды 97-122**: строчные латинские буквы от 'a' до 'z'
- **Коды 123-127**: дополнительные символы ({, |, }, ~, DEL)

**Преимущества ASCII:**
- Простота и компактность
- Широкая поддержка
- Эффективность для английского текста

**Недостатки ASCII:**
- Поддержка только латинского алфавита
- Отсутствие национальных символов (кириллица, иероглифы, диакритические знаки)

### Расширенные 8-битные кодировки

С распространением компьютеров за пределами англоязычных стран возникла необходимость в кодировании национальных алфавитов. Переход от 7 к 8 битам дал дополнительные 128 позиций (коды 128-255), которые использовались для национальных символов.

#### Windows-1251 (CP-1251)

**Windows-1251** — кодировка для кириллических алфавитов, разработанная Microsoft для операционных систем Windows.

Структура:
- **Коды 0-127**: полностью совпадают с ASCII
- **Коды 128-255**: кириллические символы, типографские знаки, специальные символы

Кодировка широко использовалась в русскоязычном секторе Интернета в 1990-2000-х годах, особенно в операционных системах Windows.

**Особенности:**
- Совместимость с ASCII в нижней половине таблицы
- Поддержка русского, украинского, белорусского и некоторых других кириллических алфавитов
- Фиксированная длина: 1 байт на символ

#### КОИ-8 (Код Обмена Информацией, 8-битный)

**КОИ-8Р** (КОИ-8 Russian) — кодировка для русского языка, разработанная в СССР в 1970-х годах.

Ключевая особенность КОИ-8: кириллические буквы размещены в таблице таким образом, что при отбрасывании восьмого бита получается соответствующая латинская буква из ASCII. Это позволяло читать русский текст на системах, не поддерживающих восьмой бит, хотя и латинскими буквами.

Например:
- Русская буква 'А' имеет код 193 (11000001₂)
- При сбросе старшего бита получаем 65 (01000001₂) — латинская 'A'

КОИ-8 была стандартной кодировкой в UNIX-системах и доминировала в российском сегменте Интернета до появления Unicode.

#### ISO 8859-5

**ISO 8859-5** — международный стандарт для кириллицы, часть серии ISO 8859.

Менее распространённая, чем Windows-1251 и КОИ-8, но иногда встречается в системах, ориентированных на международные стандарты.

### Проблема «кракозябр»

Наличие множества различных кодировок привело к серьёзной проблеме: если текст, закодированный в одной кодировке, интерпретируется программой как текст в другой кодировке, на экране появляются бессмысленные символы — **«кракозябры»** (mojibake).

Например, текст «Привет» в кодировке Windows-1251 имеет следующие коды (в шестнадцатеричной системе):
```
ï F0 E8 E2 E5 F2
```

Если эти же байты интерпретировать как UTF-8 или КОИ-8, получится набор непонятных символов.

Для решения этой проблемы необходимо либо явно указывать кодировку (например, в HTTP-заголовках, метатегах HTML), либо использовать универсальную кодировку Unicode.

## Unicode — универсальная система кодирования

### Идея Unicode

**Unicode** — международный стандарт кодирования символов, разработанный консорциумом Unicode Consortium с 1991 года. Основная цель — создать единую систему, охватывающую все письменности мира.

Unicode присваивает уникальный числовой код (называемый **code point**) каждому символу. Code point записывается в формате U+XXXX, где XXXX — шестнадцатеричное число.

Например:
- U+0041 — латинская буква 'A'
- U+0410 — кириллическая буква 'А'
- U+4E2D — китайский иероглиф 中 (zhōng)
- U+1F600 — эмодзи 😀 (grinning face)

**Версия Unicode 15.1** (сентябрь 2023 года) содержит 149 813 символов, охватывающих 161 письменность, включая современные и исторические алфавиты, математические символы, эмодзи и многое другое.

### Кодовое пространство Unicode

Unicode делит своё кодовое пространство на 17 плоскостей (planes), каждая из которых содержит 65 536 (2¹⁶) позиций:

- **Plane 0 (BMP, Basic Multilingual Plane)** — основная многоязычная плоскость (U+0000 до U+FFFF): содержит символы большинства современных языков
- **Plane 1 (SMP)** — дополнительная многоязычная плоскость: исторические письменности, эмодзи
- **Plane 2 (SIP)** — дополнительная иероглифическая плоскость: редкие китайские иероглифы
- **Planes 3-13** — зарезервированы для будущего использования
- **Plane 14 (SSP)** — специальные символы
- **Planes 15-16** — частное использование

Таким образом, всего доступно 17 × 65 536 = 1 114 112 позиций (code points), но не все они используются.

### UTF-8 (8-bit Unicode Transformation Format)

**UTF-8** — самая популярная схема кодирования Unicode, использующая переменное количество байтов (от 1 до 4) для представления символов.

**Принцип кодирования:**
- Символы ASCII (U+0000 до U+007F) кодируются одним байтом, полностью совпадая с ASCII
- Символы U+0080 до U+07FF кодируются двумя байтами
- Символы U+0800 до U+FFFF кодируются тремя байтами
- Символы U+10000 до U+10FFFF кодируются четырьмя байтами

**Структура кодирования:**

| Диапазон code points | Количество байтов | Битовая структура                    |
|----------------------|-------------------|--------------------------------------|
| U+0000 – U+007F      | 1                 | 0xxxxxxx                             |
| U+0080 – U+07FF      | 2                 | 110xxxxx 10xxxxxx                    |
| U+0800 – U+FFFF      | 3                 | 1110xxxx 10xxxxxx 10xxxxxx           |
| U+10000 – U+10FFFF   | 4                 | 11110xxx 10xxxxxx 10xxxxxx 10xxxxxx  |

Первый байт многобайтовой последовательности указывает длину последовательности количеством единиц в начале (110... — 2 байта, 1110... — 3 байта, 11110... — 4 байта). Все последующие байты начинаются с битовой последовательности 10.

**Преимущества UTF-8:**
- **Обратная совместимость с ASCII**: любой ASCII-текст является корректным UTF-8
- **Самосинхронизация**: можно определить начало символа, просматривая поток байтов
- **Компактность для латиницы**: английский текст занимает столько же места, как в ASCII
- **Отсутствие проблем с порядком байтов** (endianness)

**Недостатки UTF-8:**
- Неэффективность для текстов на языках, требующих 3-4 байта (китайский, японский)
- Переменная длина символа усложняет индексацию

**Популярность:** По данным W3Techs, более 98% веб-сайтов используют UTF-8 (2024 год). Это де-факто стандарт для Интернета.

### UTF-16 (16-bit Unicode Transformation Format)

**UTF-16** использует 2 или 4 байта для представления символов.

**Принцип кодирования:**
- Символы BMP (U+0000 до U+FFFF) кодируются одной 16-битной единицей (2 байта)
- Символы за пределами BMP (U+10000 до U+10FFFF) кодируются парой 16-битных единиц — **суррогатной парой** (4 байта)

**Суррогатные пары:**
Для кодирования символов за пределами BMP используются два специальных диапазона:
- **High surrogates**: U+D800 – U+DBFF (1024 значения)
- **Low surrogates**: U+DC00 – U+DFFF (1024 значения)

Комбинация high и low surrogate образует код символа: 1024 × 1024 = 1 048 576 дополнительных символов.

**Порядок байтов (Endianness):**
UTF-16 может быть:
- **UTF-16BE** (Big Endian): старший байт первый
- **UTF-16LE** (Little Endian): младший байт первый

Для определения порядка байтов в начале файла используется **BOM (Byte Order Mark)** — специальный символ U+FEFF:
- В UTF-16BE: FF FE
- В UTF-16LE: FE FF

**Применение:** UTF-16 используется внутри многих платформ (Java, .NET, Windows API, JavaScript), так как обеспечивает компромисс между простотой обработки и универсальностью.

**Преимущества UTF-16:**
- Большинство современных символов кодируются двумя байтами
- Эффективна для азиатских языков

**Недостатки UTF-16:**
- Несовместима с ASCII
- Проблема endianness требует BOM
- Необходимость обработки суррогатных пар усложняет код

### UTF-32 (32-bit Unicode Transformation Format)

**UTF-32** использует фиксированную длину — 4 байта (32 бита) для каждого символа.

**Принцип кодирования:**
Каждый code point Unicode напрямую представляется как 32-битное целое число. Например:
- 'A' (U+0041) → 00 00 00 41 (в шестнадцатеричном)
- '中' (U+4E2D) → 00 00 4E 2D
- '😀' (U+1F600) → 00 01 F6 00

**Преимущества UTF-32:**
- **Фиксированная длина**: каждый символ занимает ровно 4 байта
- **Простота индексации**: n-й символ находится по адресу base_address + n × 4
- **Отсутствие суррогатных пар** и других сложностей кодирования

**Недостатки UTF-32:**
- **Большой размер**: текст занимает в 4 раза больше места по сравнению с ASCII/UTF-8
- **Неэффективность**: 99% символов в большинстве текстов можно закодировать меньшим количеством байтов

**Применение:** UTF-32 редко используется для хранения и передачи данных, но иногда применяется внутри программ для упрощения обработки строк.

Как и UTF-16, UTF-32 может быть UTF-32BE или UTF-32LE.

## Расчёт размера текстовых файлов

### Общий подход

Размер текстового файла зависит от:
1. Количества символов в тексте
2. Кодировки
3. Дополнительных данных (BOM, метаинформация)

Для простых кодировок с фиксированной длиной:
```
Размер (байт) = Количество символов × Байт на символ
```

Для UTF-8 расчёт сложнее, так как разные символы занимают разное количество байтов.

### Примеры расчётов

**Пример 1:** Размер английского текста

Текст: "Hello, World!" (13 символов)

В **ASCII** или **UTF-8**:
- Все символы латинские и знаки пунктуации → 1 байт на символ
- Размер = 13 байт

В **UTF-16**:
- Каждый символ → 2 байта
- Размер = 13 × 2 = 26 байт (+2 байта BOM = 28 байт)

В **UTF-32**:
- Каждый символ → 4 байта
- Размер = 13 × 4 = 52 байта (+4 байта BOM = 56 байт)

**Пример 2:** Размер русского текста

Текст: "Привет, мир!" (12 символов)

В **Windows-1251** или **КОИ-8**:
- Все символы → 1 байт
- Размер = 12 байт

В **UTF-8**:
- Кириллические символы (10 шт.) → 2 байта каждый = 20 байт
- Знаки пунктуации и пробел (2 шт.) → 1 байт каждый = 2 байта
- Размер = 20 + 2 = 22 байта

В **UTF-16**:
- Все символы → 2 байта
- Размер = 12 × 2 = 24 байта (+2 байта BOM = 26 байт)

**Пример 3:** Смешанный текст

Текст: "Hello, мир! 你好 😀" (17 символов: 7 латинских + пробелы + знаки, 4 кириллических, 2 китайских иероглифа, 1 эмодзи)

В **UTF-8**:
- Латинские символы, пробелы, знаки (9 шт.): 1 × 9 = 9 байт
- Кириллические символы (4 шт.): 2 × 4 = 8 байт
- Китайские иероглифы (2 шт.): 3 × 2 = 6 байт
- Эмодзи (1 шт.): 4 байта
- Размер = 9 + 8 + 6 + 4 = 27 байт

В **UTF-16**:
- Латинские, кириллические, китайские (15 шт.): 2 × 15 = 30 байт
- Эмодзи (1 шт., суррогатная пара): 4 байта
- Размер = 30 + 4 = 34 байта (+2 байта BOM = 36 байт)

В **UTF-32**:
- Все символы: 4 × 17 = 68 байт (+4 байта BOM = 72 байта)

**Пример 4:** Большой текстовый файл

Книга на русском языке: 500 000 символов (примерно 250 страниц)

Предположим, что в тексте:
- 90% кириллических символов (450 000)
- 10% латинских, цифр, пунктуации, пробелов (50 000)

В **Windows-1251**:
- Размер = 500 000 байт = 488.28 КБ ≈ 0.48 МБ

В **UTF-8**:
- Кириллица: 450 000 × 2 = 900 000 байт
- Остальное: 50 000 × 1 = 50 000 байт
- Размер = 950 000 байт = 927.73 КБ ≈ 0.91 МБ

В **UTF-16LE**:
- Размер = 500 000 × 2 = 1 000 000 байт = 976.56 КБ ≈ 0.95 МБ

Вывод: для русского текста UTF-8 примерно в 2 раза больше, чем Windows-1251, но универсальнее.

**Пример 5:** Текст на китайском языке

Текст: 10 000 иероглифов

В **UTF-8**:
- Большинство китайских иероглифов → 3 байта
- Размер ≈ 10 000 × 3 = 30 000 байт ≈ 29.3 КБ

В **UTF-16**:
- Иероглифы BMP → 2 байта
- Размер = 10 000 × 2 = 20 000 байт ≈ 19.5 КБ

В **GB2312/GBK** (китайская национальная кодировка):
- Размер ≈ 10 000 × 2 = 20 000 байт ≈ 19.5 КБ

Вывод: для китайского текста UTF-16 эффективнее UTF-8.

## Практическая значимость и применение

### Выбор кодировки

При разработке программного обеспечения важно правильно выбрать кодировку:

**Используйте UTF-8:**
- Для веб-приложений и API
- Для хранения многоязычных данных
- Для файлов конфигурации и логов
- Для обмена данными между системами

**Используйте UTF-16:**
- Внутри приложений на Java, C#, JavaScript
- При работе с Windows API
- Для эффективного представления азиатских текстов

**Используйте национальные кодировки** (Windows-1251, KOI8-R):
- Только для поддержки устаревших систем
- При работе со старыми файлами

### Декларация кодировки

**В HTML:**
```html
<meta charset="UTF-8">
```

**В HTTP-заголовках:**
```
Content-Type: text/html; charset=UTF-8
```

**В XML:**
```xml
<?xml version="1.0" encoding="UTF-8"?>
```

**В Python:**
```python
# -*- coding: utf-8 -*-
```

### Преобразование между кодировками

Многие языки программирования предоставляют функции для конвертации текста между кодировками:

**Python:**
```python
# Чтение файла в Windows-1251
with open('file.txt', 'r', encoding='windows-1251') as f:
    text = f.read()

# Запись в UTF-8
with open('file_utf8.txt', 'w', encoding='utf-8') as f:
    f.write(text)
```

**Утилита iconv (Linux/Unix):**
```bash
iconv -f windows-1251 -t utf-8 input.txt -o output.txt
```

### Обработка текста в программах

При работе с текстом в программах важно:

1. **Явно указывать кодировку** при чтении и записи файлов
2. **Использовать Unicode-строки** внутри программы
3. **Валидировать входные данные** на соответствие объявленной кодировке
4. **Обрабатывать ошибки** некорректных последовательностей байтов
5. **Тестировать** на текстах различных языков

## Ключевые термины

**ASCII** — 7-битная кодировка для латинских символов (128 символов)

**Кодировка (character encoding)** — система соответствия между символами и их двоичными кодами

**Кракозябры (mojibake)** — нечитаемые символы, возникающие при интерпретации текста в неправильной кодировке

**Unicode** — универсальный стандарт кодирования, охватывающий символы всех письменностей мира

**Code point** — уникальный числовой код символа в Unicode (формат U+XXXX)

**UTF-8** — переменная кодировка Unicode (1-4 байта), совместимая с ASCII

**UTF-16** — кодировка Unicode с использованием 2 или 4 байт, использующая суррогатные пары

**UTF-32** — кодировка Unicode с фиксированной длиной 4 байта

**BOM (Byte Order Mark)** — специальный символ в начале файла, указывающий порядок байтов (U+FEFF)

**Суррогатная пара** — пара 16-битных значений в UTF-16 для кодирования символов за пределами BMP

**Windows-1251** — 8-битная кодировка для кириллицы, разработанная Microsoft

**КОИ-8** — 8-битная кодировка для кириллицы, популярная в UNIX-системах

**BMP (Basic Multilingual Plane)** — основная плоскость Unicode (U+0000 до U+FFFF)

**Endianness** — порядок следования байтов в многобайтовых представлениях (Big Endian / Little Endian)

## Контрольные вопросы

1. **Теоретический вопрос:** Объясните, почему ASCII использует 7 бит, а не 8. Какие преимущества и недостатки это даёт? Почему современные кодировки используют 8-битные байты?

2. **Практическая задача:** Рассчитайте размер текстового файла, содержащего фразу "Информатика — наука о данных" (30 символов) в следующих кодировках: а) Windows-1251, б) UTF-8, в) UTF-16LE (с BOM), г) UTF-32LE (с BOM). Покажите подробные расчёты.

3. **Проблемная ситуация:** Пользователь открыл текстовый файл и увидел набор непонятных символов вместо текста. Опишите возможные причины этой проблемы и способы её решения. Как можно определить исходную кодировку файла?

4. **Сравнительный анализ:** В чём принципиальное отличие UTF-8 от UTF-16 и UTF-32? Приведите примеры ситуаций, когда каждая из этих кодировок будет наиболее эффективна.

5. **Практическое применение:** При разработке веб-приложения, поддерживающего 10 языков (английский, русский, китайский, арабский, хинди и другие), какую кодировку вы бы выбрали и почему? Обоснуйте свой выбор, учитывая как технические, так и практические аспекты.

---

**Данная глава заложила основы понимания представления текстовой информации в компьютерах.** В следующих главах мы изучим другие аспекты представления информации, а также рассмотрим, как операционные системы и прикладное программное обеспечение работают с текстовыми данными в различных кодировках.
